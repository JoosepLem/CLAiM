jwt:
  secret: poc-secret-key-change-me-to-a-very-long-value-at-least-32-bytes
  expiration-hours: 8
app:
  security:
    secure-cookie: false
  datasource:
    migration:
      username: ${FLYWAY_USER:claim}
      password: ${FLYWAY_PASSWORD:claim}
